-# .message
-#   .upper-message
-#     .upper-message__user-name
-#       = message.user.name
-#     .upper-message__date
-#       = message.created_at.strftime("%Y/%m/%d %H:%M")
-#   .lower-message
-#     - if message.content.present?
-#       %p.lower-message__content
-#         = message.content
-#     = image_tag message.image.url, class: 'lower-message__image' if message.image.present?


.messages{data: {message: {id: message.id}}}
  .message-top__sender
    = message.user.name
  .message-top__date
    = message.created_at.strftime("%Y/%m/%d %H:%M")
  .message-top__details
    - if message.content.present?
      %p.lower-message__content
        = message.content
    = image_tag message.image.url, class: 'lower-message_image' if message.image.present?
-#     .message-bottom__sender
-#       taro
-#     .message-bottom__date
-#       2019/11/20(Wed) 17:55:30
-#     .message-bottom__details
-#       さようなら